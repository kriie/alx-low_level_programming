#include "main.h"

/**
 * add - function that adds two integers,
 * @num1: number one,
 * @num2: number two,
 * Return: the result
 */

int add(int num1, int num2)
{
	int result = (num1 + num2);

	return result;
}
