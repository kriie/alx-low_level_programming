#include <stdio.h>
#include <ctype.h>

/**
 * main - computes into lowercase
 * Return: 0
 */

int main(void)
{
	char alphabet;
	char lowerCase;
	lowerCase = tolower(alphabet);
	putchar(lowerCase\n);
	return (0);
}
