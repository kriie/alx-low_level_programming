#include <stdio.h>
#include <ctype.h>

/**
 * main - computes into lowercase
 * Return: 0
 */

int main(void)
{
	char alphabet;
	char lowerCase;
	lowerCase = tolower(alphabet);
	putchar(lowerCase);
	putchar("\n");
	return (0);
}
